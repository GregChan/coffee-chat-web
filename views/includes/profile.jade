include ../mixins/editable-text.jade
div(class="section white profile-section profile-edit")
	if title
		div=title
	div(class="row container")
		div(class="col s12 m3 l3")
			a(href=profile.linkedInProfile)
				img(class="responsive-img circle coffee-chat-circle" src=profile.profilePic)
			h5(class="center text-grey") #{profile.firstName} #{profile.lastName}
			+editableTextInput(profile.headLine, 0, "center", {})
		div(class="col s12 m3 l3")
			if profile.positions
				h5(class="section-title text-orange") Work Experience
				div(class="spacer-15")
				for work in profile.positions.work
					div(data-work-experience)
						+editableTextInput(work.title, work.positionID, "title", {"data-position":"data"})
						+editableTextInput(work.company, work.companyID, "subtitle", {"data-company":"data"})
		div(class="col s12 m3 l3")
			if profile.positions
				h5(class="section-title text-orange") Education
				div(class="spacer-15")
					div(data-education)
						for edu in profile.positions.education
							+editableTextInput(edu.title, edu.positionID, "title", {"data-position":"data"})
							+editableTextInput(edu.school, edu.schoolID, "subtitle", {"data-company":"data"})
		div(class="col s12 m3 l3" data-interests)
			h5(class="section-title text-orange") Interests
			if interests && interests.fields
				for field in interests.fields
					for item in field.values
						div #{item.name}